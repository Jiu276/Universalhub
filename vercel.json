{
  "builds": [
    {
      "src": "universalhub-site/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/universalhub-site/dist/index.html"
    }
  ]
}

